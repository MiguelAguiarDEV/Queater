APP_NAME=Queater
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://queater.miguelaguiar.es

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Base de datos SQLite para producción
DB_CONNECTION=sqlite
DB_DATABASE=database.sqlite

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=.miguelaguiar.es
SESSION_SECURE_COOKIES=true

BROADCAST_CONNECTION=reverb
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# Cache usando archivo para simplicidad
CACHE_STORE=file
CACHE_PREFIX=queater_

# Configuración de correo para producción
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=your-smtp-host.com
MAIL_PORT=587
MAIL_USERNAME=your-email@domain.com
MAIL_PASSWORD=your-email-password
MAIL_FROM_ADDRESS="noreply@queater.miguelaguiar.es"
MAIL_FROM_NAME="${APP_NAME}"
MAIL_ENCRYPTION=tls

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Reverb para producción
REVERB_APP_ID=163104
REVERB_APP_KEY=xvtc0aukmbjbshhin26o
REVERB_APP_SECRET=sog5mjp15c16sil3za31
REVERB_HOST="localhost"
REVERB_PORT=8080
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

# Configuraciones adicionales para producción
TRUSTED_PROXIES=*
TRUSTED_HOSTS=^queater\.miguelaguiar\.es$
